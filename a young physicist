#include <stdio.h>

int main()
{
    int a,b,c,d,sum=0;
    scanf("%d",&a);
    for(b=0; b<a; b++)
    {
        for(c=0; c<3; c++)
        {
            scanf("%d",&d);
            sum=sum+d;
        }
    }
    if(sum==0)
        printf("YES");
    else
        printf("NO");
    return 0;
}
